
name := "dyno"

scalaVersion in Global := "2.11.0-RC1"

version in Global := "0.1-SNAPSHOT"

organization in Global := "ch.epfl.lamp"

organizationName in Global := "LAMP/EPFL (Scala Team)"

organizationHomepage in Global := Some(url("http://epfl.ch"))

licenses in Global := Seq("BSD-style" -> url("http://scala-miniboxing.org/license.html"))

homepage in Global := Some(url("http://lamp.epfl.ch"))
